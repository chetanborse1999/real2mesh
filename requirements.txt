# Install the hardware-appropriate build of these versions first.
torch==2.8.0
torchvision==0.23.0

# Image and array processing
numpy==2.5.1
Pillow==11.3.0

# OpenCV 5.0.0 is installed from conda-forge by the README command. The
# test_env build is not published on PyPI as opencv-python==5.0.0.

# Mesh loading and interactive viewing
trimesh==4.12.2
pyglet==1.5.27

# Explicit SAM2 runtime dependencies
hydra-core==1.3.4
iopath==0.1.10
tqdm==4.67.1

# The official SAM2 checkout must exist at ./sam2 before installation.
-e ./sam2
